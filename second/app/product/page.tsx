import { title } from "process"

export default function product(){
    return(
        <div className="">
            Product will show here.
        </div>
    )
}

export const metadata = {
    title:"Product"
}